dist: bionic
sudo: required
language: python
services:
  - docker
cache:
  pip: true
script:
  - pip3 install -U -r test-requirements.txt && pylint -E *py && pytype *py && PYTHONPATH=. coverage run test_pipette.py && coverage report pipette.py
  - docker build -f Dockerfile . -t anarkiwi/pipette
